import React from 'react';

export const TableContext = React.createContext(null);
export const TableHeadContext = React.createContext(null);
export const TableRowContext = React.createContext(null);
